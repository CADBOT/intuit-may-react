var numbers = [1 , 10, 15]

var newNumbers = []

for (var i = 0; i < numbers.length; i++) {
  newNumbers[i] = numbers[i] + 100
}

var otherNewNumbers = numbers.map(function(number) {
  return number + 100
})

function (number)

=> (number)

(number) =>


number =>

var otherNewNumbers = numbers.map(number  => number + 100)
